apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: myapp-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: xn----7sbhdycib3bahm3ad4m.xn--p1ai
    http:
      paths:
        - path: /otusapp($|/)(.*)
          backend:
            serviceName: myapp-service
            servicePort: 8000